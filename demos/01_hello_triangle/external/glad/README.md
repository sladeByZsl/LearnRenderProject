# GLAD

This is a small GLAD-compatible loader for the first LearnOpenGL lesson.

It only declares and loads the OpenGL functions used by `01_hello_triangle`.
For later lessons, replace it with a full GLAD OpenGL 3.3 Core generated
package from https://glad.dav1d.de/.
